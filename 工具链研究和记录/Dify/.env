# ------------------------------------------------------------------
# Docker Compose 部署的基本默认配置。
# 仅包含服务启动所需的变量。
#
# 对于默认部署，请将此文件复制为 .env 并运行：
#   docker compose up -d
#
# 可选和特定提供商的变量位于 docker/envs/ 目录下。
# 当您需要这些高级设置时，将可选的 *.env.example 文件复制为去掉 .example 后缀的同名文件。
# docker/.env 中的值会覆盖 docker/envs/*.env 文件中的值。
# ------------------------------------------------------------------

# 核心服务地址
# 控制台 API 的外部访问地址（留空则自动推导）
CONSOLE_API_URL=
# 服务内部使用的控制台 API 地址
SERVER_CONSOLE_API_URL=http://api:5001
# 控制台 Web 页面的外部访问地址（留空则自动推导）
CONSOLE_WEB_URL=
# 服务 API 的外部访问地址（留空则自动推导）
SERVICE_API_URL=
# 触发器服务的基础 URL
TRIGGER_URL=http://localhost
# 应用 API 的外部访问地址（留空则自动推导）
APP_API_URL=
# 应用 Web 页面的外部访问地址（留空则自动推导）
APP_WEB_URL=
# 文件访问的外部 URL（留空则自动推导）
FILES_URL=
# 内部文件访问地址（留空则自动推导）
INTERNAL_FILES_URL=
# Webhook 端点地址模板，{hook_id} 会被替换为实际 hook ID
ENDPOINT_URL_TEMPLATE=http://localhost/e/{hook_id}
# 前端使用的 WebSocket 连接地址
NEXT_PUBLIC_SOCKET_URL=ws://localhost

# 运行时和安全设置
# 系统语言环境
LANG=C.UTF-8
# 系统语言环境（LC_ALL）
LC_ALL=C.UTF-8
# Python 标准 I/O 编码
PYTHONIOENCODING=utf-8
# uv 包管理器的缓存目录
UV_CACHE_DIR=/tmp/.uv-cache
# 密钥，留空则在存储目录中自动生成持久化的密钥
SECRET_KEY=
# 初始管理员密码
INIT_PASSWORD=
# 部署环境，PRODUCTION 表示生产环境
DEPLOY_ENV=PRODUCTION
# 检查更新的地址
CHECK_UPDATE_URL=https://updates.dify.ai
# OpenAI 兼容 API 的基础地址
OPENAI_API_BASE=https://api.openai.com/v1
# 是否启用数据库迁移
MIGRATION_ENABLED=true
# 文件访问令牌的超时时间（秒）
FILES_ACCESS_TIMEOUT=300
# 是否启用协作模式（多人实时编辑）
ENABLE_COLLABORATION_MODE=true
# 若要从 COMPOSE_PROFILES 中移除 `collaboration`，可停止专用的 WebSocket 服务

# 日志和服务进程
# 日志级别
LOG_LEVEL=INFO
# 日志输出格式（text 或 json）
LOG_OUTPUT_FORMAT=text
# 日志文件路径
LOG_FILE=/app/logs/server.log
# 单个日志文件的最大大小（MB）
LOG_FILE_MAX_SIZE=20
# 保留的日志文件备份数量
LOG_FILE_BACKUP_COUNT=5
# 日志日期格式
LOG_DATEFORMAT=%Y-%m-%d %H:%M:%S
# 日志时区
LOG_TZ=UTC
# 是否启用调试模式
DEBUG=false
# Flask 调试开关
FLASK_DEBUG=false
# 是否启用请求日志记录
ENABLE_REQUEST_LOGGING=False
# Dify API 服务绑定的地址
DIFY_BIND_ADDRESS=0.0.0.0
# Dify API 服务监听的端口
DIFY_PORT=5001
# 服务 worker 数量
SERVER_WORKER_AMOUNT=1
# 服务 worker 类型（gevent 为异步协程）
SERVER_WORKER_CLASS=gevent
# 每个 worker 的最大连接数
SERVER_WORKER_CONNECTIONS=10
# WebSocket API 的 worker 类（支持 WebSocket）
API_WEBSOCKET_WORKER_CLASS=geventwebsocket.gunicorn.workers.GeventWebSocketWorker
# WebSocket worker 的最大连接数
API_WEBSOCKET_WORKER_CONNECTIONS=1000
# WebSocket gunicorn 超时时间（秒）
API_WEBSOCKET_GUNICORN_TIMEOUT=360
# gunicorn 超时时间（秒）
GUNICORN_TIMEOUT=360
# Celery worker 类型（留空使用默认）
CELERY_WORKER_CLASS=
# Celery worker 数量
CELERY_WORKER_AMOUNT=4
# 是否启用 Celery 自动伸缩
CELERY_AUTO_SCALE=false
# Celery 自动伸缩时的最大 worker 数
CELERY_MAX_WORKERS=
# Celery 自动伸缩时的最小 worker 数
CELERY_MIN_WORKERS=
# 是否禁用 worker 健康检查
COMPOSE_WORKER_HEALTHCHECK_DISABLED=true
# worker 健康检查间隔
COMPOSE_WORKER_HEALTHCHECK_INTERVAL=30s
# worker 健康检查超时时间
COMPOSE_WORKER_HEALTHCHECK_TIMEOUT=30s

# 数据库
# 数据库类型（postgresql）
DB_TYPE=postgresql
# 数据库用户名
DB_USERNAME=postgres
# 数据库密码
DB_PASSWORD=difyai123456
# 数据库主机名
DB_HOST=db_postgres
# 数据库端口
DB_PORT=5432
# 数据库名称
DB_DATABASE=dify
# SQLAlchemy 连接池大小
SQLALCHEMY_POOL_SIZE=30
# 连接池允许的最大溢出连接数
SQLALCHEMY_MAX_OVERFLOW=10
# 连接回收时间（秒）
SQLALCHEMY_POOL_RECYCLE=3600
# 是否输出 SQLAlchemy 调试日志
SQLALCHEMY_ECHO=false
# 连接前是否发送 ping 检测连接有效性
SQLALCHEMY_POOL_PRE_PING=false
# 连接池是否使用 LIFO 策略
SQLALCHEMY_POOL_USE_LIFO=false
# 从连接池获取连接的超时时间（秒）
SQLALCHEMY_POOL_TIMEOUT=30
# 连接返回连接池时的重置策略
SQLALCHEMY_POOL_RESET_ON_RETURN=rollback
# PostgreSQL 数据目录
PGDATA=/var/lib/postgresql/data/pgdata
# PostgreSQL 最大连接数
POSTGRES_MAX_CONNECTIONS=200
# PostgreSQL 共享缓冲区大小
POSTGRES_SHARED_BUFFERS=128MB
# PostgreSQL 工作内存大小
POSTGRES_WORK_MEM=4MB
# PostgreSQL 维护工作内存大小
POSTGRES_MAINTENANCE_WORK_MEM=64MB
# PostgreSQL 有效缓存大小
POSTGRES_EFFECTIVE_CACHE_SIZE=4096MB
# PostgreSQL 语句超时时间（0 表示禁用）
POSTGRES_STATEMENT_TIMEOUT=0
# PostgreSQL 空闲事务超时时间（0 表示禁用）
POSTGRES_IDLE_IN_TRANSACTION_SESSION_TIMEOUT=0

# Redis 和 Celery
# Redis 主机
REDIS_HOST=redis
# Redis 端口
REDIS_PORT=6379
# Redis 用户名
REDIS_USERNAME=
# Redis 密码
REDIS_PASSWORD=difyai123456
# 是否启用 Redis SSL 连接
REDIS_USE_SSL=false
# Redis SSL 证书验证要求
REDIS_SSL_CERT_REQS=CERT_NONE
# Redis SSL CA 证书路径
REDIS_SSL_CA_CERTS=
# Redis SSL 证书文件路径
REDIS_SSL_CERTFILE=
# Redis SSL 私钥文件路径
REDIS_SSL_KEYFILE=
# Redis 数据库编号
REDIS_DB=0
# Redis 键前缀
REDIS_KEY_PREFIX=
# Redis 最大连接数（留空使用默认）
REDIS_MAX_CONNECTIONS=
# Redis 重试次数
REDIS_RETRY_RETRIES=3
# Redis 重试退避基数（秒）
REDIS_RETRY_BACKOFF_BASE=1.0
# Redis 重试退避上限（秒）
REDIS_RETRY_BACKOFF_CAP=10.0
# Redis 套接字超时时间（秒）
REDIS_SOCKET_TIMEOUT=5.0
# Redis 连接超时时间（秒）
REDIS_SOCKET_CONNECT_TIMEOUT=5.0
# Redis 健康检查间隔（秒）
REDIS_HEALTH_CHECK_INTERVAL=30
# Celery Broker URL
CELERY_BROKER_URL=redis://:difyai123456@redis:6379/1
# Celery 结果后端
CELERY_BACKEND=redis
# Celery Broker 是否使用 SSL
BROKER_USE_SSL=false
# Celery 任务注解（JSON 格式或留空）
CELERY_TASK_ANNOTATIONS=null
# 事件总线的 Redis URL（留空则使用默认值）
EVENT_BUS_REDIS_URL=
# 事件总线 Redis 通道类型
EVENT_BUS_REDIS_CHANNEL_TYPE=pubsub
# 是否使用 Redis 集群作为事件总线
EVENT_BUS_REDIS_USE_CLUSTERS=false
# 事件总线监听器加入超时时间（毫秒）
EVENT_BUS_LISTENER_JOIN_TIMEOUT_MS=2000

# Web 与应用限制
# API 跨域允许的来源（* 表示所有来源）
WEB_API_CORS_ALLOW_ORIGINS=*
# 控制台跨域允许的来源
CONSOLE_CORS_ALLOW_ORIGINS=*
# Cookie 所属的域名（留空则自动使用当前域名）
COOKIE_DOMAIN=
# 前端使用的 Cookie 域名
NEXT_PUBLIC_COOKIE_DOMAIN=
# 前端批处理并发数
NEXT_PUBLIC_BATCH_CONCURRENCY=5
# API Sentry DSN 地址（用于错误监控）
API_SENTRY_DSN=
# API Sentry 事务采样率
API_SENTRY_TRACES_SAMPLE_RATE=1.0
# API Sentry 性能剖析采样率
API_SENTRY_PROFILES_SAMPLE_RATE=1.0
# Web 前端 Sentry DSN 地址
WEB_SENTRY_DSN=
# Amplitude 分析 API Key
AMPLITUDE_API_KEY=
# 文本生成超时时间（毫秒）
TEXT_GENERATION_TIMEOUT_MS=60000
# 内容安全策略白名单
CSP_WHITELIST=
# 是否允许页面被嵌入 iframe
ALLOW_EMBED=false
# 是否允许内联样式
ALLOW_INLINE_STYLES=false
# 是否允许不安全的 data: 协议
ALLOW_UNSAFE_DATA_SCHEME=false
# Top-K 检索的最大值
TOP_K_MAX_VALUE=10
# 索引时最大分段 token 长度
INDEXING_MAX_SEGMENTATION_TOKENS_LENGTH=4000
# 循环节点的最大执行次数
LOOP_NODE_MAX_COUNT=100
# 最大工具数量
MAX_TOOLS_NUM=10
# 最大并行执行限制
MAX_PARALLEL_LIMIT=10
# 最大迭代次数
MAX_ITERATIONS_NUM=99
# 最大树深度
MAX_TREE_DEPTH=50
# 是否启用 Jinareader 网页抓取
ENABLE_WEBSITE_JINAREADER=true
# 是否启用 Firecrawl 网页抓取
ENABLE_WEBSITE_FIRECRAWL=true
# 是否启用 Watercrawl 网页抓取
ENABLE_WEBSITE_WATERCRAWL=true
# 是否启用单美元符号 LaTeX 渲染
NEXT_PUBLIC_ENABLE_SINGLE_DOLLAR_LATEX=false
# 是否开启仍在开发中的预览功能（目前包括“快速跳转”命令面板中的 /create 和 /refine 命令）
NEXT_PUBLIC_ENABLE_FEATURE_PREVIEW=false
# 是否启用 VineXT 实验性功能
EXPERIMENTAL_ENABLE_VINEXT=false

# 存储与默认向量数据库
# 文件存储类型（opendal 为统一文件抽象层）
STORAGE_TYPE=opendal
# OpenDAL 方案（fs 表示本地文件系统）
OPENDAL_SCHEME=fs
# 本地文件系统存储根目录
OPENDAL_FS_ROOT=storage
# 默认向量存储类型（weaviate）
VECTOR_STORE=weaviate
# 向量索引名称前缀
VECTOR_INDEX_NAME_PREFIX=Vector_index
# Weaviate 服务端点
WEAVIATE_ENDPOINT=http://weaviate:8080
# Weaviate API Key
WEAVIATE_API_KEY=WVF5YThaHlkYwhGUSmCRgsX3tD5ngdN8pkih
# Weaviate gRPC 端点
WEAVIATE_GRPC_ENDPOINT=grpc://weaviate:50051
# Weaviate 分词方式
WEAVIATE_TOKENIZATION=word
# Weaviate 数据持久化路径
WEAVIATE_PERSISTENCE_DATA_PATH=/var/lib/weaviate
# Weaviate 查询默认返回数量限制
WEAVIATE_QUERY_DEFAULTS_LIMIT=25
# 是否启用匿名访问
WEAVIATE_AUTHENTICATION_ANONYMOUS_ACCESS_ENABLED=true
# 默认向量化模块（none 表示不使用）
WEAVIATE_DEFAULT_VECTORIZER_MODULE=none
# Weaviate 集群节点主机名
WEAVIATE_CLUSTER_HOSTNAME=node1
# 是否启用 API Key 认证
WEAVIATE_AUTHENTICATION_APIKEY_ENABLED=true
# 允许的 API Key 列表
WEAVIATE_AUTHENTICATION_APIKEY_ALLOWED_KEYS=WVF5YThaHlkYwhGUSmCRgsX3tD5ngdN8pkih
# API Key 对应的用户列表
WEAVIATE_AUTHENTICATION_APIKEY_USERS=hello@dify.ai
# 是否启用管理员列表授权
WEAVIATE_AUTHORIZATION_ADMINLIST_ENABLED=true
# 管理员用户列表
WEAVIATE_AUTHORIZATION_ADMINLIST_USERS=hello@dify.ai
# 是否禁用 Weaviate 遥测
WEAVIATE_DISABLE_TELEMETRY=false
# 是否启用中文分词（GSE）
WEAVIATE_ENABLE_TOKENIZER_GSE=false
# 是否启用日文分词（Kagome）
WEAVIATE_ENABLE_TOKENIZER_KAGOME_JA=false
# 是否启用韩文分词（Kagome）
WEAVIATE_ENABLE_TOKENIZER_KAGOME_KR=false

# 代码沙箱与 SSRF 代理
# 代码执行服务的端点
CODE_EXECUTION_ENDPOINT=http://sandbox:8194
# 代码执行服务的 API Key
CODE_EXECUTION_API_KEY=dify-sandbox
# 是否验证代码执行服务的 SSL 证书
CODE_EXECUTION_SSL_VERIFY=True
# 代码执行连接池最大连接数
CODE_EXECUTION_POOL_MAX_CONNECTIONS=100
# 连接池中最大 Keep-Alive 连接数
CODE_EXECUTION_POOL_MAX_KEEPALIVE_CONNECTIONS=20
# Keep-Alive 连接过期时间（秒）
CODE_EXECUTION_POOL_KEEPALIVE_EXPIRY=5.0
# 代码执行连接超时时间（秒）
CODE_EXECUTION_CONNECT_TIMEOUT=10
# 代码执行读取超时时间（秒）
CODE_EXECUTION_READ_TIMEOUT=60
# 代码执行写入超时时间（秒）
CODE_EXECUTION_WRITE_TIMEOUT=10
# 沙箱 API Key
SANDBOX_API_KEY=dify-sandbox
# Gin 框架运行模式（release 为生产模式）
SANDBOX_GIN_MODE=release
# 沙箱工作进程超时时间（秒）
SANDBOX_WORKER_TIMEOUT=15
# 是否启用沙箱网络访问
SANDBOX_ENABLE_NETWORK=true
# 沙箱 HTTP 代理地址
SANDBOX_HTTP_PROXY=http://ssrf_proxy:3128
# 沙箱 HTTPS 代理地址
SANDBOX_HTTPS_PROXY=http://ssrf_proxy:3128
# 沙箱监听端口
SANDBOX_PORT=8194
# PyPI 镜像地址（留空使用官方源）
PIP_MIRROR_URL=
# SSRF 代理 HTTP 地址
SSRF_PROXY_HTTP_URL=http://ssrf_proxy:3128
# SSRF 代理 HTTPS 地址
SSRF_PROXY_HTTPS_URL=http://ssrf_proxy:3128
# SSRF 代理 HTTP 端口
SSRF_HTTP_PORT=3128
# SSRF 代理核心转储目录
SSRF_COREDUMP_DIR=/var/spool/squid
# SSRF 反向代理端口
SSRF_REVERSE_PROXY_PORT=8194
# SSRF 代理访问的沙箱主机名
SSRF_SANDBOX_HOST=sandbox
# SSRF 默认超时时间（秒）
SSRF_DEFAULT_TIME_OUT=5
# SSRF 默认连接超时时间（秒）
SSRF_DEFAULT_CONNECT_TIME_OUT=5
# SSRF 默认读取超时时间（秒）
SSRF_DEFAULT_READ_TIME_OUT=5
# SSRF 默认写入超时时间（秒）
SSRF_DEFAULT_WRITE_TIME_OUT=5
# SSRF 连接池最大连接数
SSRF_POOL_MAX_CONNECTIONS=100
# SSRF 连接池最大 Keep-Alive 连接数
SSRF_POOL_MAX_KEEPALIVE_CONNECTIONS=20
# SSRF Keep-Alive 连接过期时间（秒）
SSRF_POOL_KEEPALIVE_EXPIRY=5.0

# 插件守护进程
# 插件专用数据库名
DB_PLUGIN_DATABASE=dify_plugin
# 对外暴露的插件守护进程端口
EXPOSE_PLUGIN_DAEMON_PORT=5002
# 插件守护进程监听端口
PLUGIN_DAEMON_PORT=5002
# 插件守护进程密钥
PLUGIN_DAEMON_KEY=lYkiYYT6owG+71oLerGzA7GXCgOT++6ovaezWAjpCjf+Sjc3ZtU+qUEi
# 插件守护进程访问地址
PLUGIN_DAEMON_URL=http://plugin_daemon:5002
# 插件包最大大小（字节）
PLUGIN_MAX_PACKAGE_SIZE=52428800
# 插件模型 Schema 缓存时间（秒）
PLUGIN_MODEL_SCHEMA_CACHE_TTL=3600
# 是否启用插件性能分析（pprof）
PLUGIN_PPROF_ENABLED=false
# 插件调试监听的地址
PLUGIN_DEBUGGING_HOST=0.0.0.0
# 插件调试端口
PLUGIN_DEBUGGING_PORT=5003
# 对外暴露的插件调试主机
EXPOSE_PLUGIN_DEBUGGING_HOST=localhost
# 对外暴露的插件调试端口
EXPOSE_PLUGIN_DEBUGGING_PORT=5003
# Dify 内部 API Key（用于插件调用 Dify API）
PLUGIN_DIFY_INNER_API_KEY=QaHbTe77CtuXmsfyhR7+vRjI/+XbV1AaFy691iy+kGDv2Jvy0/eAh8Y1
# Dify 内部 API 地址
PLUGIN_DIFY_INNER_API_URL=http://api:5001
# 是否强制验证签名
FORCE_VERIFYING_SIGNATURE=true
# 插件标准 I/O 缓冲区大小
PLUGIN_STDIO_BUFFER_SIZE=1024
# 插件标准 I/O 最大缓冲区大小
PLUGIN_STDIO_MAX_BUFFER_SIZE=5242880
# Python 环境初始化超时时间（秒）
PLUGIN_PYTHON_ENV_INIT_TIMEOUT=120
# 插件最大执行超时时间（秒）
PLUGIN_MAX_EXECUTION_TIMEOUT=600
# 插件文件存储类型（local 为本地存储）
PLUGIN_STORAGE_TYPE=local
# 本地插件存储根目录
PLUGIN_STORAGE_LOCAL_ROOT=/app/storage
# 插件工作路径
PLUGIN_WORKING_PATH=/app/storage/cwd
# 插件安装路径
PLUGIN_INSTALLED_PATH=plugin
# 插件包缓存路径
PLUGIN_PACKAGE_CACHE_PATH=plugin_packages
# 插件媒体缓存路径
PLUGIN_MEDIA_CACHE_PATH=assets
# 对象存储 Bucket 名称（使用 OSS 时填写）
PLUGIN_STORAGE_OSS_BUCKET=
# 是否启用插件 Sentry 错误监控
PLUGIN_SENTRY_ENABLED=false
# 插件 Sentry DSN 地址
PLUGIN_SENTRY_DSN=
# 是否启用插件市场
MARKETPLACE_ENABLED=true
# 插件市场 API 地址
MARKETPLACE_API_URL=https://marketplace.dify.ai
# 插件市场前端地址（留空使用默认）
MARKETPLACE_URL=

# Nginx 与 Docker Compose
# Nginx 服务器名称
NGINX_SERVER_NAME=_
# 是否启用 HTTPS
NGINX_HTTPS_ENABLED=false
# HTTP 监听端口
NGINX_PORT=80
# HTTPS 监听端口
NGINX_SSL_PORT=443
# SSL 证书文件名
NGINX_SSL_CERT_FILENAME=dify.crt
# SSL 证书密钥文件名
NGINX_SSL_CERT_KEY_FILENAME=dify.key
# SSL 协议版本
NGINX_SSL_PROTOCOLS=TLSv1.2 TLSv1.3
# Nginx worker 进程数
NGINX_WORKER_PROCESSES=auto
# 客户端请求体最大大小
NGINX_CLIENT_MAX_BODY_SIZE=100M
# Keep-Alive 超时时间（秒）
NGINX_KEEPALIVE_TIMEOUT=65
# 代理读取超时时间
NGINX_PROXY_READ_TIMEOUT=3600s
# 代理发送超时时间
NGINX_PROXY_SEND_TIMEOUT=3600s
# 是否启用 Certbot 质询
NGINX_ENABLE_CERTBOT_CHALLENGE=false
# Socket.IO 上游服务地址
NGINX_SOCKET_IO_UPSTREAM=api_websocket:5001
# 对外暴露的 HTTP 端口
EXPOSE_NGINX_PORT=80
# 对外暴露的 HTTPS 端口
EXPOSE_NGINX_SSL_PORT=443
# Docker Compose 启用的配置组合（根据 VECTOR_STORE 和 DB_TYPE 自动拼接）
COMPOSE_PROFILES=${VECTOR_STORE:-weaviate},${DB_TYPE:-postgresql},collaboration