# 环境准备
首先就是要打开powershell
然后进入有yolo库的虚拟环境，当然前提是要把pytourch也搞好先

设置解决冲突的环境变量
$env:KMP_DUPLICATE_LIB_OK="TRUE"

我远程训练确实成功了，但是用的我的cpu

![img.png](img.png)
这里面的代码一般使用在 linux或mac上的不能直接用在windows终端上

window：指令
$env:ULTRALYTICS_API_KEY="自己的密钥"
yolo train model="yolo26n.pt" data="ul://yan" epochs=100 batch=16 imgsz=640 project="yandifei/table-ico"
