你是一个精通微信小程序 UI 开发与交互设计的前端专家。现在需要创建一个名为“微信小程序AI聊天界面”的 skill，该 skill 用于在微信小程序中快速生成一个类手机端 DeepSeek 软件风格的 AI 用户交互界面。

请严格按以下步骤执行：
1. 首先，完整阅读当前文件夹下提供的微信小程序 AI 模板，学习其中所有的页面结构、组件封装、样式写法、数据流方式以及控件交互细节。
2. 结合你已有的 UI 设计 skills 和 skill 创建规范（creat skills），抽象出核心的聊天界面控件与布局模式。
3. 生成最终的 skill 文件（.skills 格式），该 skill 需要包含以下能力：
   - 对话列表的渲染（区分用户消息与 AI 回复，支持 Markdown/代码块展示）
   - 底部输入区域（文本输入框、发送按钮、可扩展功能区占位，如语音、图片等）
   - 自动滚动到底部、消息状态（发送中/失败）、流式输出的 UI 动效
   - 顶部导航栏（标题、清空对话、返回等）
   - 适配微信小程序暗黑模式与响应式布局
   - 使用微信小程序原生框架（WXML + WXSS + JS/TS），不依赖第三方 UI 库
4. 输出的 skill 应结构清晰、可复用，包含必要的属性配置（如初始欢迎语、占位提示文字、头像样式等），方便开发者通过简单配置即可集成到不同项目中。
5. 最终将生成的文件命名为“微信小程序AI聊天界面.skills”，并确保该 skill 的描述和标签中明确注明“微信小程序”、“AI聊天”、“DeepSeek风格”等关键词。     